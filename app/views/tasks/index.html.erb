<h1>My tasks</h1>
<% @tasks.each do |task| %>
<ul><li><%= link_to "Delete",
  task_path(task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
<input type="checkbox"><%= link_to task.title, task_path(task)%></li>
</ul>
<% end %>

<%= link_to "create a new task", new_path %>
